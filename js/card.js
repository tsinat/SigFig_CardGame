export default class Card {
    constructor(value, name, suit){
        this.value = value;
        this.name = name;
        this.suit = suit;
    }
    // method to graphically draw the card in the DOM
    drawCard(card) {
        
    }
}
